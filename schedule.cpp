#include "schedule.hpp"

using namespace std;


std::unordered_map<int, Game[10]> Schedule::create_schedule(std::unordered_map<Team*, int>& league){

    for (size_t i = 0; i < 10; i++)
    {
        
        // this->schedule_table.insert
        // for each round create 10 different games s.t. in 1 season, each 2 teams play each other twice.
        // for(auto& it : league){

        // }
    }
    






}